/**
 * Тест точности детекции дублирования для UAK v2.0
 * Проверяет корректность работы улучшенного алгоритма
 */

const { isAutoGenerated, detectCodeDuplication } = require('./smart-analyzer.cjs');
const fs = require('fs');
const path = require('path');

// Тестовые данные для проверки точности
const TEST_CASES = [
  {
    name: 'DockerAnalyzer',
    file: '../eap-analyzer/src/analyzers/docker-analyzer.ts',
    expectedType: 'ANALYZER',
    shouldHaveDuplication: false, // Похожие проверки - это норма для анализатора
    description: 'Анализатор Docker - должен пройти без ложного дублирования',
  },
  {
    name: 'EMTAnalyzer',
    file: '../eap-analyzer/src/analyzers/emt-analyzer.ts',
    expectedType: 'ANALYZER',
    shouldHaveDuplication: false, // Типичные паттерны анализатора
    description: 'EMT анализатор - должен пройти без ложного дублирования',
  },
  {
    name: 'UserService (реальный дублирующий код)',
    content: `
    export class UserService {
      validateEmail(email) {
        if (!email) return false;
        return email.includes('@');
      }

      validateEmail2(email) {
        if (!email) return false;
        return email.includes('@');
      }

      process() { /* уникальная логика */ }
    }`,
    expectedType: 'SERVICE',
    shouldHaveDuplication: true, // Реальное дублирование функций
    description: 'Сервис с реальным дублированием - должен детектировать',
  },
  {
    name: 'ValidatorCollection (норма для валидаторов)',
    content: `
    export class ValidatorCollection {
      validateRequired(value) {
        return value != null && value !== '';
      }

      validateEmail(email) {
        return email && email.includes('@');
      }

      validatePhone(phone) {
        return phone && phone.length >= 10;
      }
    }`,
    expectedType: 'VALIDATOR',
    shouldHaveDuplication: false, // Похожие валидации - норма
    description: 'Коллекция валидаторов - похожие паттерны это норма',
  },
];

// Тест автогенерированных файлов
const AUTO_GENERATED_TESTS = [
  {
    name: 'SvelteKit app.js',
    filepath: '.svelte-kit/generated/client/app.js',
    content: `import * as client_hooks from '../../../src/hooks.client.ts';\n\nexport { matchers } from './matchers.js';\nexport const nodes = [\n\t() => import('./nodes/0'),\n\t() => import('./nodes/1')\n];`,
    shouldBeFiltered: true,
  },
  {
    name: 'Regular TypeScript file',
    filepath: 'src/services/UserService.ts',
    content: `export class UserService {\n  getUserById(id: string) {\n    return this.database.find(id);\n  }\n}`,
    shouldBeFiltered: false,
  },
];

console.log('🧪 ТЕСТ ТОЧНОСТИ УЛУЧШЕНИЙ SMART ANALYZER v2.0');
console.log('='.repeat(60));

let totalTests = 0;
let passedTests = 0;

// Тестируем автогенерированные файлы
console.log('\n🔍 ТЕСТ 1: Фильтрация автогенерированных файлов');
console.log('-'.repeat(50));

for (const test of AUTO_GENERATED_TESTS) {
  totalTests++;
  const isFiltered = isAutoGenerated(test.filepath, test.content);
  const passed = isFiltered === test.shouldBeFiltered;

  console.log(`${passed ? '✅' : '❌'} ${test.name}`);
  console.log(`   Путь: ${test.filepath}`);
  console.log(`   Ожидается фильтрация: ${test.shouldBeFiltered}`);
  console.log(`   Фактически отфильтрован: ${isFiltered}`);

  if (passed) passedTests++;
  console.log('');
}

// Тестируем детекцию дублирования
console.log('\n🔍 ТЕСТ 2: Контекстная детекция дублирования');
console.log('-'.repeat(50));

for (const test of TEST_CASES) {
  totalTests++;

  let content;
  if (test.content) {
    content = test.content;
  } else {
    try {
      const fullPath = path.join(__dirname, test.file);
      content = fs.readFileSync(fullPath, 'utf8');
    } catch (error) {
      console.log(`⚠️  Файл ${test.file} не найден, пропускаем тест`);
      continue;
    }
  }

  const duplicationScore = detectCodeDuplication(content, test.expectedType);
  const hasDuplication = duplicationScore > 0;
  const passed = hasDuplication === test.shouldHaveDuplication;

  console.log(`${passed ? '✅' : '❌'} ${test.name} (${test.expectedType})`);
  console.log(`   ${test.description}`);
  console.log(`   Ожидается дублирование: ${test.shouldHaveDuplication}`);
  console.log(
    `   Детектировано дублирование: ${hasDuplication} (score: ${duplicationScore.toFixed(3)})`
  );

  if (passed) passedTests++;
  console.log('');
}

// Итоговая статистика
console.log('\n📊 ИТОГОВАЯ СТАТИСТИКА');
console.log('-'.repeat(50));
const accuracy = ((passedTests / totalTests) * 100).toFixed(1);
console.log(`Прошло тестов: ${passedTests}/${totalTests}`);
console.log(`Точность: ${accuracy}%`);

if (accuracy >= 90) {
  console.log('🎉 ОТЛИЧНО! Smart Analyzer готов к продакшену');
} else if (accuracy >= 80) {
  console.log('⚠️  ХОРОШО, но нужны улучшения для продакшена');
} else {
  console.log('❌ ТРЕБУЮТСЯ ЗНАЧИТЕЛЬНЫЕ УЛУЧШЕНИЯ');
}

console.log('\n🚀 Тест завершен');
